# 系统架构总览

## 基本信息
- 当前版本：v1.0.0
- 封装方式：便携版（绿色免安装）
- 运行环境：Windows + Python 3.8+
- AI引擎：DeepSeek API
- 本地存储：SQLite
- 操作界面：Flask本地Web应用
- 操作方式：双击.bat文件
- 代码仓库：https://github.com/Fat-designer920/rural-revitalization-kb （公开，可直接读取）

## 架构

用户操作层(.bat文件, GBK编码, 调用Python前chcp 65001)
  |
Python脚本层(scripts/, UTF-8编码)
  |- config_wizard.py - tkinter配置向导
  |- setup.py - 一键初始化
  |- file_reader.py - 多格式读取(PDF/Word/Excel/图片/文本)
  |- deepseek_client.py - DeepSeek API封装(费用控制+3次重试)
  |- preprocessor.py - 智能预处理(AI重命名+标签)
  |- extractor.py - 知识点提取(5种Prompt)
  |- api_server.py - Flask审核界面(读取HTML到内存,Response直接返回)
  |- db_manager.py - 数据库CRUD
  |- check_system.py - 系统检查
  |- prompts/prompt_templates.py - 10个Prompt
  |
数据层(SQLite, 8张表)
  |- categories(27条) | source_files | knowledge_points
  |- knowledge_versions(v1.1.0) | architecture_suggestions(v1.1.0)
  |- operation_logs | api_call_logs | notion_sync_log(v1.2.0)

## 数据流
1. data/pending/ -> preprocessor(AI重命名) -> data/processing/
2. extractor(AI提取) -> SQLite(待审核) -> data/completed/
3. api_server(审核界面) -> 人工审核 -> SQLite(已确认)

## 安全：Fernet加密Key | 白名单路径 | 每日费用上限 | 强制HTTPS

## 技术注意事项
- bat必须GBK编码+CRLF换行
- api_server.py用Response返回HTML(不用render_template)
- review.html不能有emoji字符,JS用ES5 Promise语法
- Python输出前需chcp 65001 + PYTHONIOENCODING=utf-8

## 协作体系
- Claude Projects：6个项目文件维护系统状态，每次迭代后更新
- GitHub仓库：公开仓库存储代码，Claude可直接读取最新版本
- 三层配合：Projects记录"改了什么" + GitHub记录"代码长什么样" + 对话中实时协作

## 交付状态
- v1.0.0 核心版 - 已完成
- v1.1.0 增强版(架构扩充/去重/搜索/版本管理) - 待开发
- v1.2.0 完整版(Notion同步/备份恢复/知识图谱) - 待开发
